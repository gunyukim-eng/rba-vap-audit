# 신규협력사 점검 — AI 자동판정 기능 기획서 (v0.1, 기획 단계)

> 상태: **기획만** (구현 착수 전). 문서 사진·인터뷰(녹음/기록)를 올리면 Claude API가
> 각 점검 항목의 등급(Conformance/Minor/Major/Priority/N/A)을 **자동 제안**하고,
> 감사자가 검토·확정하는 흐름을 설계한다.

---

## 1. 목표 & 범위

- 대상: 현재 앱의 **신규협력사 점검** 탭 (19개 항목, 판정기준·배점 내장, Yes/No 플로우).
- 하고자 하는 것
  1. **문서 사진 업로드** → 해당 항목 판정기준과 대조해 **자동 등급 제안**.
  2. **인터뷰 녹음/기록 업로드** → 전사 후 판정기준과 대조해 **문항 자동응답 + 등급 제안**.
- 하지 않는 것(중요): AI가 **최종 판정을 확정하지 않는다.** 감사 무결성을 위해
  등급은 항상 **감사자가 검토·수정·확정**한다. AI는 "초안 + 근거"만 제공.

---

## 2. 핵심 제약 (설계를 결정짓는 사실)

| 제약 | 내용 | 설계 영향 |
|---|---|---|
| **비전 O** | Claude는 이미지(사진/스캔) 네이티브 입력. Opus 4.8 고해상도 2576px. | 문서 사진을 그대로 판정에 투입 가능 |
| **오디오 X** | Claude는 오디오 파일을 직접 못 받음 | 인터뷰는 **STT(음성→텍스트) 선처리** 필수 |
| **API 키 노출 금지** | static `index.html`에 키를 넣으면 즉시 유출 | **백엔드 프록시(서버리스)** 필수 |
| **구조화 출력** | `output_config.format`(JSON 스키마)로 등급·근거를 안정적 수신 | 파싱·검증 없이 결과 사용 |
| **PII 민감도** | 근로자 인터뷰·급여명세 등 민감정보 | 동의·보존정책·마스킹 설계 필요 |
| **감사 무결성** | AI 판정을 그대로 기록하면 감사 신뢰성 훼손 | Human-in-the-loop 확정 단계 필수 |

---

## 3. 아키텍처

```
[모바일/브라우저: index.html]
   │  1) 사진/오디오/전사 업로드 (multipart)
   ▼
[백엔드 프록시 (서버리스 함수)]        ← ANTHROPIC_API_KEY 는 여기에만 존재
   │  2) (오디오면) STT로 전사
   │  3) 항목 판정기준 + 증거 → Claude Messages API (구조화 출력)
   ▼
[Claude API  claude-opus-4-8]
   │  4) {grade, confidence, 문항응답[], 근거[], 인용} JSON 반환
   ▼
[백엔드] → [프론트]  5) 등급 제안 카드 렌더 → 감사자 검토/확정 → localStorage 저장
```

- **프론트**: 기존 앱 그대로. "AI 판정 요청" 버튼 + 증거 업로드 UI + 제안 결과 카드만 추가.
- **백엔드 후보**: Cloudflare Workers / Vercel Functions / 간단한 Node·Python 서비스 중 택1.
  (지금 배포가 정적 호스팅이면 서버리스 함수 하나 추가가 가장 가벼움.)
- **STT 후보**: Whisper(자체호스팅) 또는 클라우드 STT. *Claude와 무관한 별도 컴포넌트*.
  1차엔 **전사 텍스트 직접 붙여넣기/업로드**만 지원하고, 자동 STT는 2차로 미룰 수 있음.

---

## 4. 데이터 플로우 (증거 유형별)

### 4-1. 문서 사진 (예: 근로계약서, 급여명세서, 취업규칙 사진)
1. 감사자가 항목(예: A0103 모국어 계약서)에서 사진 N장 업로드.
2. 백엔드가 이미지(base64 또는 Files API)를 항목 **판정기준 전문 + 문항**과 함께 Claude에 전달.
3. Claude가 사진에서 관련 사실을 읽고(계약서 언어/서명/필수기재 등) 판정기준에 매핑.
4. `{grade, 문항별 yes/no/na, 근거문장, 이미지 내 확인 위치 설명}` 반환.

### 4-2. 인터뷰 (녹음 또는 기록)
1. 녹음 업로드 시 → 백엔드에서 **STT 전사** → 전사 텍스트 확보.
   (또는 감사자가 이미 작성한 인터뷰 기록 텍스트를 바로 업로드)
2. 전사 텍스트 + 항목 판정기준·문항을 Claude에 전달.
3. Claude가 근로자/경영진 진술을 문항에 매핑해 자동응답 + 등급 제안 + **인용(어느 발언 근거인지)**.

---

## 5. 자동판정 설계 (프롬프트 & 출력 스키마)

### 입력 구성
- **system**: "너는 RBA VAP 신규협력사 심사 보조자다. 제공된 판정기준에만 근거해 판정하고,
  근거가 부족하면 추측하지 말고 `insufficient_evidence`로 표시하라." (+ 등급 정의)
- **user content**: `[이미지들…] + 항목 메타(id, 소분류, 구분, 배점표) + 판정기준 전문 + 문항 목록(Q1..Qn, 각 sev)`
- 판정기준·문항은 이미 앱의 `NSUP_ITEMS` / `NSUP_Q`에 있으므로 그대로 직렬화해 전달.

### 출력 (구조화, `output_config.format` json_schema)
```jsonc
{
  "item_id": "A0103",
  "suggested_grade": "minor",          // conformance|minor|major|priority|na|insufficient_evidence
  "confidence": 0.0,                    // 0~1
  "answers": [                          // 기존 Yes/No 플로우에 매핑
    { "q_id": "Q1", "answer": "yes", "evidence": "계약서에 모국어(베트남어) 병기 확인" },
    { "q_id": "Q2", "answer": "no",  "evidence": "필수기재 중 '근로시간' 누락" }
  ],
  "rationale": "…판정기준의 Minor ① 적용…",
  "citations": ["이미지2 상단 계약 조항", "인터뷰 05:12 근로자 진술"],
  "needs_human_review": true
}
```
- 반환된 `answers`를 그대로 `S.nsupAns[itemId]`에 **미리 채워** 두고, `suggested_grade`는 배지로 표시.
- 감사자가 문항을 수정하면 기존 로직대로 등급이 재계산됨 → **AI 제안과 사람 확정이 자연스럽게 합쳐짐**.

### 모델·파라미터 (권장 기본값)
- **모델**: `claude-opus-4-8` (판정 정확도 우선). 대량·비용 민감 시 `claude-sonnet-5`로 조정.
- **thinking**: `{type:"adaptive"}`, **effort**: `high`~`max` (오판 비용이 큼).
- **출력**: `output_config.format`(json_schema). *주의: 구조화 출력은 citations 기능과 동시 사용 시 400* →
  인용은 스키마 안 `citations` 필드로 받는다(문서 인용 기능을 따로 켜지 않음).
- **대용량/다중 이미지**: 요청이 크면 streaming 사용, PDF·재사용 파일은 Files API(beta).

---

## 6. 기존 Yes/No 플로우와의 연동

- 항목 질문 화면(`screenNsupItem`)에 **"📷 증거로 자동판정"** 버튼 추가.
- AI 응답 도착 시:
  - 각 문항 버튼에 **AI 제안값**을 옅게 프리필 + "AI" 태그 표시(사람이 누른 값과 구분).
  - 상단에 `AI 제안 등급 · confidence · 근거 펼치기` 카드.
  - 감사자가 **확정** 눌러야 실제 점수/총점에 반영(그 전엔 "제안" 상태).
- 이렇게 하면 **현재 데이터 모델·채점 로직을 바꾸지 않고** 위에 얹을 수 있음.

---

## 7. 보안 · 프라이버시 (감사 도구라 특히 중요)

- **API 키**: 백엔드 환경변수(`ANTHROPIC_API_KEY`)에만. 프론트/깃에 절대 금지.
- **PII**: 근로자 인터뷰·개인서류는 민감정보. 필요 시 **업로드 전 마스킹**(이름/주민번호),
  **보존기간 제한**, **협력사·근로자 동의** 절차 명시.
- **데이터 보존**: Anthropic 데이터 처리 정책 확인. 민감 감사면 ZDR/보존설정 검토.
  (일부 최신 모델은 ZDR 미지원 → 모델 선택 시 함께 고려)
- **접근통제**: 백엔드에 인증(감사자만 호출) + 요청 로깅.

---

## 8. 단계별 로드맵

- **MVP (1차)**
  - 백엔드 프록시 함수 1개 + `claude-opus-4-8` 비전 판정.
  - **문서 사진**만 지원, **인터뷰는 전사 텍스트 직접 업로드**만 지원(자동 STT 제외).
  - 항목 1개 단위 "자동판정 요청" → 제안 등급/문항 프리필 → 감사자 확정.
- **2차**
  - 오디오 업로드 + **자동 STT** 파이프라인.
  - 다중 이미지·PDF(Files API), 인용 하이라이트, confidence 임계값 정책.
- **3차**
  - 항목 일괄 판정, 근거 리포트 자동 생성, CAP(시정조치) 초안 연계.

---

## 9. 결정 필요 (사용자 확인 사항)

1. **배포/백엔드**: 어디에 서버리스 함수를 둘지 (Cloudflare / Vercel / 사내 서버 등).
2. **STT 제공자**: 자체 Whisper vs 클라우드 STT (또는 1차엔 전사 직접 입력으로 시작).
3. **모델 등급**: 정확도 우선(opus-4-8) vs 비용 우선(sonnet-5).
4. **PII 정책**: 마스킹 범위·보존기간·동의 문구 — 사내 규정 기준 필요.
5. **자동화 수준**: "제안만"으로 갈지, 고신뢰(confidence↑) 항목은 자동확정 허용할지.

---

*작성: 기획 단계 문서. 구현 착수 시 이 문서를 기준으로 백엔드 스켈레톤부터 시작.*
